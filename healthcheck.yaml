---
apiVersion: kuberhealthy.github.io/v2
kind: HealthCheck
metadata:
  name: http-content-check
  namespace: kuberhealthy
spec:
  runInterval: 5m
  timeout: 5m
  podSpec:
    spec:
      containers:
        - name: http-content-check
          image: ghcr.io/kuberhealthy/http-content-check:v0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: TARGET_URL
              value: "https://example.com"
            - name: TARGET_STRING
              value: "example"
            - name: TIMEOUT_DURATION
              value: "30s"
      restartPolicy: Never
